# 开发日志 — 2026年5月29日

## 今日完成

### 项目初始化
- [x] 项目需求分析与确认
- [x] 技术方案确定（PWA 网页应用）
- [x] 创建项目目录结构
- [x] 编写需求文档 `docs/requirements.md`
- [x] 编写技术规范 `docs/tech-spec.md`
- [x] 编写设计规范 `docs/design-spec.md`
- [x] 编写执行步骤 `docs/implementation-steps.md`
- [x] 创建 CLAUDE.md 项目指引

### 基础骨架
- [x] HTML 骨架（index.html：标题栏 + 三板块 + Tab导航 + 通用弹窗）
- [x] 全局 CSS（蓝白配色 + CSS 变量 + 三板块样式）
- [x] App 入口（Tab 切换、数据库初始化、模块事件绑定）

### 存储层
- [x] IndexedDB 封装（4张表：tasks / notes / timeline_entries / expenses）
- [x] 通用 CRUD + 按日期查询 + 各表便捷 API

### 设计大升级 🎨
- [x] 色彩系统重构：蓝灰渐变背景 + 暖棕点缀 + CSS 点阵纹理
- [x] 衬线字体（Noto Serif SC）提升文艺感
- [x] 卡片左侧装饰条 + 柔和多层阴影
- [x] 毛玻璃 Tab 导航栏
- [x] 背景三色渐变 `EDF3F8 → F5F0EB → F8F6F3`
- [x] 活动分类 + 记账分类颜色体系

### 新增记账功能 💰
- [x] 板块③ 改为双视图：子 Tab「时间轴」+「记账」
- [x] 收支记录（9个分类：餐饮/交通/购物/健身/娱乐/工资/奖金等）
- [x] 当日收支汇总卡片（收入/支出分色显示）
- [x] 记账表单弹窗（类型切换 + 分类选择 + 金额 + 备注）
- [x] 删除记录 + 日期联动

## 待办事项

- [x] 日历页：添加任务弹窗（图标选择 + 时间选择 + 动态训练参数 + 颜色选择）
- [x] 心得页：新建/编辑弹窗（标题 + 正文 + 心情选择器）
- [x] 时间轴：添加/编辑活动弹窗（时间范围 + 图标）
- [x] 17个 SVG 健身图标设计（蓝白线条风格）
- [x] PWA manifest + Service Worker
- [x] PWA 图标生成（杠铃主题，192×192 + 512×512）
- [x] 测试与打磨：移动端适配、表单统一、交互优化

## 全部10步已完成 🎉

## 备注

- 所有代码注释使用中文
- 背景渐变 + 点阵纹理实现「文艺手帐」风格，无需外部图片
- 记账与时间轴共享日期选择器，数据按日期隔离
- **新增**：日历添加任务弹窗支持根据动作类型动态显示训练参数
  - 力量训练：重量(kg) + 组数
  - 有氧训练（跑步）：速度(km/h) + 时长(分钟)
  - 有氧训练（爬坡）：速度 + 坡度(%) + 时长
  - 有氧训练（爬楼）：档位 + 时长
  - 柔韧/核心：时长
- **新增**：图标库从14个扩展至17个（新增爬坡、爬楼、高位下拉）
- **新增**：任务详情区显示训练参数摘要（如「60kg × 4组」）
- **新增**：心得页完整实现 — 新建/编辑弹窗
  - 10种心情表情选择器（😊😄😌🤔😤😢💪🔥🧘😴）
  - 日期选择 + 标题 + 正文（textarea）
  - 搜索功能完善：输入关键词实时过滤列表
  - 编辑模式复用同一弹窗，自动填充已有数据
- **新增**：时间轴活动弹窗 — 新建/编辑/删除
  - 分组图标选择器（17个图标按部位排列）
  - 时间范围选择（开始/结束时间 + 校验）
  - 6色标签选择 + 备注字段
  - 默认时间=当前整点，结束=开始+1h

- **新增**：17个 SVG 健身图标（蓝白线条风格，24×24 viewBox）
  - 每个动作有独特的线条图标（stroke="#6B9FD4" stroke-width="2"）
  - 图标选择器、任务详情、活动块全部切换为 SVG 显示
  - emoji 仅保留作为回退方案
  - IconsModule 新增 `getSVG(id, size)` 方法

- **新增**：PWA 支持（Step 9）
  - `manifest.json`：应用清单（standalone 模式，暖白背景 + 品牌蓝主题色）
  - `sw.js`：Service Worker（Cache First 策略，预缓存 13 个静态资源）
  - `icons/icon-192.png` + `icons/icon-512.png`：杠铃主题应用图标
  - `index.html`：激活 manifest 链接 + Apple Touch Icon
  - `js/app.js`：Service Worker 注册 + 更新检测
  - 支持离线访问：首次加载后，断网也能打开应用

- **新增**：测试与打磨（Step 10）
  - CSS 媒体查询修复：`left:50% + right:0` 居中Bug → `margin:auto` 居中
  - 移动端安全区域适配（`env(safe-area-inset-bottom)`）
  - 新增 `form-select` 样式（自定义下拉箭头，与 form-input 统一风格）
  - 时间轴弹窗时间输入框统一使用 `form-input` 类
  - 记账弹窗金额/备注/分类统一使用 `form-input` / `form-select` 类
  - ESC 键关闭弹窗（app.js 全局监听）
  - IndexedDB `onblocked` 处理（多标签页版本升级冲突提示）
  - 日历任务列表点击 → 编辑弹窗（复用 showAddForm，支持编辑/删除）
  - 编辑弹窗预填所有字段（图标、颜色、时间、训练参数）
  - 心得搜索增加 300ms 防抖（减少高频 IndexedDB 查询）

### Bug 修复（5月29日第二轮）
- [x] 日历&心得标题改为可选（去掉必填校验，placeholder改为「标题（可选）」）
- [x] 记账保存重构：hidden radio → JS变量 `selectedType`，避免 `:checked` 失效
- [x] 记账保存增加 try/catch 错误提示
- [x] **IndexedDB 版本升级 v1→v2**：确保 expenses 表在旧版 DB 中也能创建
- [x] 记账 save/addRecord/render 全链路添加 console.log 调试日志
- [x] 统一遮罩关闭逻辑：移除三个模块各自 `{ once: true }` 监听，改为 app.js 统一注册
